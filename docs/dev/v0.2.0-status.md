# v0.2.0 build status (handoff)

This file is the resume point for whoever continues the v0.2.0 build (human or cloud agent). Update it in every commit that changes the status.

**Current gates (2026-09-06, final verification round 4):** `node scripts/check.mjs` OK — 144 files syntax-checked, 3 JSON files, versions aligned at 0.2.0, 34 skills linted, 0 warnings. `node tests/run.mjs` — **1123 passed / 0 failed (43 files)**. The per-stage "Gates green:" lines below are historical: each records what the gates said at that stage, not today's numbers.

## Where the plan lives
- `docs/dev/v0.2.0-plan.md` — the approved plan (phases 0–6, file lists, verification). Follow it literally; items marked TO-VERIFY/UNVERIFIED must be checked against live vendor docs before coding them.
- `docs/dev/v0.2.0-api-digest.md` — auto-extracted API notes from the implementers of the foundation stage (exports, CLI flags, JSON keys, open issues). Read before touching `scripts/`.

## Done (commit fa9c08a on main, 2026-09-05)
- Phase 0 wiring: `Task`→`Agent`, `${CLAUDE_PLUGIN_ROOT}` script paths, agents preload skills (`skills:`), dispatch envelope sections, M21 redefined as AI discovery (`skills/seo-ai-discovery`), M22 agent-readiness (`skills/seo-agent-readiness`), `skills/seo-fix-apply` stub, M20 dispatched, `references/routing.md` + `references/ai-crawlers.md` updated, `schema/jsonld-templates/`, CHANGELOG + versions 0.2.0.
- Phase 1 (complete): `scripts/lib/*` (html tokenizer, entities, jsonld, fetch, robots+Content-Signal, bots, sitemaps, urlnorm, hreflang, store, renderers, site, pool, lang, passages, schema-lite, validate-finding, finding, bands), `scripts/snapshot.mjs`, `scripts/check.mjs`, all legacy scripts importable (`main` + `isMain`), `--snapshot` input mode, `node:test` suite (390 tests) with `tests/helpers/server.mjs`.
- Phase 4 partial: `score.mjs` integrity (4e weights, 4f calibration in `references/scoring-model.md` + scoring docs EN/ES, 4i gating/validation/unscored/activation/environment/rollup), bilingual heuristics (4h), Content-Signal parsing in `lib/robots.mjs` (4a parse part), `lib/bots.mjs` UA table (4c).

## Done (build stage: crawl, GEO scripts, platform detection, Phase-4 skill content — 2026-09-05)
- Phase 2a: `scripts/crawl.mjs` + `scripts/lib/templates.mjs` (URL template patterns, JSON-LD/body-class typing, grouping) with e2e crawl tests; the two `lib/site.mjs` fixes from the digest are applied.
- Phase 4 scripts: `ai-eligibility.mjs`, `ua-diff.mjs`, `ai-discovery.mjs`, `acp-feed-lint.mjs`, `agent-readiness.mjs`, `probe-report.mjs`, `gsc-ai-import.mjs`, with fixtures and unit tests.
- Phase 3a–c: `scripts/detect-platform.mjs` + `scripts/lib/platform-rules.mjs`, hidden `skills/seo-platform-detect/SKILL.md`, `references/platforms/*` cards, Platform-layer table filled in `references/routing.md`, platform fixtures + tests.
- Phase 4 skill content pass: findings/honesty wording for M14, M18 agentic sub-checks, severities and Google alignment in M11/M12/M5/M16/M4, M2 cloaking id, M7 bands, `geo` --probe/--gsc flows.

## Done (build stage: checks registry — 2026-09-05)
- Phase 2b `scripts/checks/*.mjs`: 25 checks on the `{id, module, ids[], scope, run(ctx)}` contract, `index.mjs` exporting `CHECKS` (frozen), `runChecks(ctx)` (never throws; per-page error isolation; `stats` with per-module counts, needs_api/manual_review/not_applicable/dropped + dropped_findings[]/errors), `loadRunContext(runDir, options)`, `knownIds()` (151 ids — `M2.shopify.seo_hidden_detected` was removed in the round-1 verifier pass, so the count is one below the original plan) and `MAX_HTML_BYTES`; `_shared.mjs` helpers are the only path to `makeFinding`; adds `scripts/lib/imgsize.mjs` (`imageSize`, `sniffFormat`, `OG_IMAGE_MIN`). Includes cross-URL, platform-conditional and GEO checks.

## Done (build stage: runner — report, audit, action, CI — 2026-09-05)
- Phase 2c/2e: `scripts/report.mjs` (merge/dedupe of deterministic + agent findings, schema-validated `report.json`, bilingual `report.md`, top-15 prioritized actions) and `scripts/audit.mjs` (exit 0/1/2/3, ndjson/md output, `--fail-under`), plus `action.yml` and `.github/workflows/{ci,seo-audit-example}.yml`; e2e tests for both runners (`tests/e2e/{report,audit}.test.mjs`) with `tests/helpers/serve.mjs` and the agents fixture.

## Done (verify/fix pass — 2026-09-05)
- Cross-cutting verification of the checks registry and runners: 13 issues found and fixed across `scripts/checks/{_shared,cwv,index,links-site,platform-shopify}.mjs`, `scripts/{crawl,report}.mjs`, `scripts/lib/site.mjs`, with regression coverage added in `tests/unit/{checks,geo-scripts}.test.mjs` and `tests/e2e/{audit,crawl}.test.mjs`; skill/reference wording corrected in `skills/{geo,seo-meta-onpage,seo-sitemaps}` and `references/platforms/shopify.md`. Gates green: 103 files syntax-checked, versions aligned at 0.2.0, 673 tests passing.

## Done (verify/fix pass, round 2 — 2026-09-06)
- Round-2 verifier issues resolved across the checks registry, scoring and reporting: fixes in `scripts/{audit,report,score,psi-client,ai-discovery,agent-readiness}.mjs`, `scripts/lib/util.mjs`, `scripts/checks/*` (shared helpers, cwv, schema, sitemaps, images, freshness, international, rendering, ecommerce, links-site, agent-readiness, agentic-commerce, ai-discovery, platform-wordpress, index), plus `schema/audit-report.schema.json`, `references/{routing,scoring-model}.md`, bilingual `docs/{en,es}/{mcp,scoring}.md` and the affected skills. Regression coverage added in `tests/unit/{checks,score,geo-scripts,util,importability}.test.mjs` and `tests/e2e/{checks-run,report}.test.mjs`. Gates green: 104 files syntax-checked, versions aligned at 0.2.0, 33 skills linted, 703 tests passing.

## Done (build stage: fix-apply foundations — adapter libs, route-map, local-files, instructions — 2026-09-06)
- Phase 3d–f (partial): `scripts/lib/adapter.mjs` (plan/manifest versions, change-class and status vocabularies with a frozen transition map, deterministic `changeId`, `makeChange`/`validateChange` per phase, data-dir resolution, `openFixRun`/`loadFixRun`/`listFixRuns` with plan, manifest, preview/before/after dirs, backups and a redacting log, and the TTL confirmation-ticket flow `issueTicket`/`checkTicket`/`consumeTicket`/`requireTicket`/`pruneTickets`).
- `scripts/lib/credentials.mjs`: 16-key catalog with aliases onto the existing `lib/platform-rules` names, `resolveKey`/`presence`/`missingKeys`/`keysForAdapter`, and `redact`/`redactObject`/`explainKeys` so secret values never reach logs or output.
- `scripts/lib/{route-map,http,diff,frontmatter,shell}.mjs` plus the first two adapters, `scripts/adapters/{local-files,instructions}.mjs`, with repo fixtures under `tests/fixtures/repos/` and unit suites for each.
- Gates green: 122 files syntax-checked, versions aligned at 0.2.0, 33 skills linted, 836 tests passing.

## Done (build stage: adapters, guards, fix flow — 2026-09-06)
- Phase 3d–h (rest, complete): `scripts/adapters/{_shared,shopify-theme,shopify-admin,wordpress-rest,wordpress-wpcli,page-api}.mjs` plus read-only `scripts/adapters/providers/{bigcommerce,ghost,hubspot,webflow,wix}.mjs`, each on the capabilities/plan/preview/apply/verify/rollback contract with staged-by-default writes, injectable `execImpl`/`fetchImpl`, credential redaction and ticket-gated apply/publish.
- `scripts/fix-plan.mjs` and `scripts/fix-ticket.mjs` drive the end-to-end fix run (report -> plan -> ticket -> preview/apply/verify/rollback); `scripts/guard-bash.mjs` plus the containment rewrite of `scripts/guard-write.mjs` are wired as the two `hooks/hooks.json` matchers.
- `skills/fix/SKILL.md` flow rewrite and the full `skills/seo-fix-apply/SKILL.md` protocol, `.claude-plugin/plugin.json` `userConfig`, `.mcp.json.example` (+ @shopify/dev-mcp), `agents/seo-fixer-writer.md`.
- New suites `tests/unit/{adapter-shopify,adapter-wordpress,adapter-page-api,fix-plan,guards}.test.mjs` with `tests/fixtures/shopify-theme/`. Gates green: 141 files syntax-checked, versions aligned at 0.2.0, 33 skills linted, 997 tests passing.

## Done (verify/fix pass — adapters and fix flow, 2026-09-06)
- Round-3 verification of the adapter layer: 9 issues found and fixed across `scripts/adapters/{_shared,instructions,local-files,shopify-admin,shopify-theme,wordpress-wpcli}.mjs`, `scripts/fix-plan.mjs` and `scripts/lib/adapter.mjs`, with regression coverage added in `tests/unit/{adapter,adapter-shopify,adapter-wordpress,fix-plan,instructions}.test.mjs` and the adapter sections of bilingual `docs/{en,es}/architecture.md` corrected to match the real contract. Gates green: 141 files syntax-checked, versions aligned at 0.2.0, 33 skills linted, 1006 tests passing.

## Done (build stage: open-issue fixes, compare command, skill reconciliation — 2026-09-06)
- Open issues from the round-3 digest resolved in the adapter layer: `updateManifest()` in `scripts/lib/adapter.mjs` is now the single reporting path for every adapter apply/rollback/publish (dry_run, applied/rolled_back/failed lists, per-change `results` map, `last_op` and `<op>_at` stamps), `loadFixRun(ref)` accepts a run id or a directory, and `scripts/adapters/_shared.mjs` gains `opThrew()` (a throw now marks the change `failed` and persists the message), `logOpEvent()` (logs `refused` when a ticket refusal covers every row) and `withheldProposedNote()`.
- Phase 5: `scripts/compare.mjs` + `skills/compare/SKILL.md`.
- Coverage accounting added to `scripts/fix-plan.mjs` (`classifyFindings`, `buildCoverage`, `renderCoverage`, `ACTIONABLE_STATUSES`, `INCLUDE_PROPOSED_HINT`): the CLI result, `plan.coverage`, each group and the printed summary now report findings/actionable/considered/planned, skipped proposed, advisory, unroutable and a per-adapter breakdown; `manifest.counts` carries the same considered/proposed totals.
- Phase 6 skill reconciliation: every `<!-- reconcile-flags-v0.2 -->` line checked against the real CLIs and removed (none remain in `skills/`).

## Done (Phase 6 docs — EN half + release hygiene — 2026-09-06)
- `README.md` rewritten against the real tree: the five-command Usage block with the actual flags, a new **"Works on any site"** support matrix (Platform · Detected · Extra checks · Apply method · Preview/staging · Credentials · Status, from the cards' Status headers), Data tiers rewritten around the scripts as the acquisition layer (tier 1 = auto-detected headless Chrome / opt-in Playwright / `--rendered-file` / `PSI_API_KEY`; tier 2 = GSC/Merchant + the manual generative-AI export), "Get cited by AI engines" updated (the Google eligibility statement naming *"AI features and your website"*, Content-Signal, UCP/agentic-commerce readiness, `--probe` with its disclaimer, `--gsc-ai-export`), the honesty-guardrail additions (only `established` failures cap · probes never scored · coverage floor and provisional bands · unknown-language pages `manual_review`), Optional scripts → **"Acquisition layer, CI action & headless runner"** with the real commands and the Action snippet, an updated project structure, and the stale "30 assertions" line replaced. The ES summary section is untouched (heading kept for the ES pass).
- `docs/en/usage.md` — all five commands with real flags and examples, the run/data-dir layout (`latest.json`, `baseline.json`, the fix-run tree), `score` from persisted runs, the `fix --target` flow with the confirmation/ticket model and the per-platform never-live-by-default rules, the four `compare` modes, the CI action, and credentials via `/plugin` `userConfig` or env.
- `docs/en/architecture.md` — the `Agent` tool and the dispatch envelope, PageSnapshot v2 on disk, persistence, the runner, the checks registry, the agents table with `skills:` preloads, the site profile, the adapters (six ops, `updateManifest`, coverage accounting) and the guard model with only the containment claims the code actually makes.
- `docs/en/mcp.md` — the three tiers as they are actually measured, `--rendered-file` as the bridge for MCP-captured DOMs, the renderer discovery order and both hint strings, PSI honesty incl. `origin_fallback`, the manual GSC import, and the optional `@shopify/dev-mcp` docs oracle.
- `docs/en/distribution.md` — the two channels plus CI, the **three** version locations enforced by `check.mjs`, and an explicit skills-only caveat (`npx skills add` ships `skills/**`, not `scripts/`, so those installs fall back to prompt-only mode).
- New `docs/en/platforms.md` — detector layers, the full support matrix, credential setup per platform (no invented vendor menu paths), per-platform quick starts, "what never goes live by default", and the UNVERIFIED table drawn from each card's §12.
- `CONTRIBUTING.md` — tests layout and the no-network rule, `check.mjs` as the cheap gate, checks-registry conventions, a new **Adding an adapter** section (six ops, injectable I/O, `updateManifest` as the only reporting path, `before` capture, credentials by key name, downgrade-not-fail), the knowledge-card template, and the AUTO-strategy rule pinned to `AUTO_STRATEGIES`.
- `CHANGELOG.md` — `[0.2.0] — 2026-09-06` with full Added/Changed/Fixed/**Security** sections derived from `git log 788e7b4..HEAD` and this file; `[0.1.0] — 2026-06-01` kept verbatim.
- Gates green: 144 files syntax-checked, versions aligned at 0.2.0, 34 skills linted, 1077 tests passing.
- Staleness note: the EN docs now quote hard constants (25 checks / 151 finding ids / 119 platform-neutral; CAP_VALUE 40, COVERAGE_FLOOR 50, REGRESSION_DELTA -2, TICKET_TTL 15 min, DEFAULT_KEEP 20; crawl DEFAULTS 12/40/2/3; 34 skills, 24 platform cards, 17 userConfig keys; the AI weight table). Changing any of them means updating the prose in `README.md` and `docs/en/*` (and the ES mirrors once written).

## Done (Phase 6 docs — ES half — 2026-09-06)
- `docs/es/{usage,architecture,mcp,distribution}.md` rewritten and new `docs/es/platforms.md` added, mirroring the EN half file-for-file and section-for-section against the real tree (same flags, same run/fix-run layout, same two-score model and axis states, same adapter contract and guard model, same tiers and credential rules, same support matrix and UNVERIFIED tables); the `README.md` Spanish summary section was updated in lockstep.
- Documentation only: no API, CLI, flag, export or schema changed.
- Gates green: 144 files syntax-checked, versions aligned at 0.2.0, 34 skills linted, 1077 tests passing.
- Staleness note now covers both halves: the hard constants quoted in `README.md` and `docs/en/*` are quoted in `docs/es/*` too — changing one means updating both mirrors.

## Done (final verification rounds — false-claim fixes and report localisation — 2026-09-06)
- Round 1 fixed the issues found against live runs (docs/en, docs/es, `references/scoring-model.md`, `schema/audit-report.schema.json`, `scripts/{ai-discovery,compare,crawl,fix-plan,report,score}.mjs`, `scripts/lib/site.mjs`, `skills/{compare,score}/SKILL.md` and six test files).
- Round 2 fixed three false claims a live run made about real sites, plus the localisation gap the reports carried:
  - `scripts/lib/jsonld.mjs` gains `isBackReference()`, `isReferenceStub()`, `ownProps()` and `pathProperty()`. A node reached through `itemReviewed` / `isSimilarTo` / `isRelatedTo` / `isVariantOf` (or `mainEntity` under a review node) POINTS AT an item declared elsewhere, and a nested node carrying nothing but `url`/`name`/`@id` is a pointer too. `scripts/checks/schema.mjs` uses both, so a review widget's `aggregateRating.itemReviewed` no longer reads as a Product missing its Offer (verified live on allbirds.com, where the page's ProductGroup does carry price 110 USD).
  - `scripts/checks/ecommerce.mjs` picks the primary product node (`primaryProduct()`) and binds the offer to that same node (`offerFor()`), instead of describing `products[0]` — a url-only `hasVariant` stub — with a third node's price and availability. `M18.variants.no_productgroup` counts declarations only.
  - `scripts/checks/platform-wordpress.mjs`: `THIN_ARCHIVE_RE` anchors its date branch at both ends, so `/2026/09/05/<slug>/` (the most common permalink structure) is no longer reported as a thin date archive with "set the date archives to noindex" (verified live on techcrunch.com).
  - `--lang es` now reaches everything a reader sees around the numbers: `scripts/score.mjs` carries `INTERPRETATIONS_ES` plus a `MESSAGES` catalog (both languages, one params object each) and takes an optional `lang`; `scripts/report.mjs` carries `REPORT_MESSAGES` for its own warnings and passes the language into the scorer. Finding text stays English by design — it comes from the checks — and that rule is written in `docs/{en,es}/scoring.md` and `docs/{en,es}/usage.md`.
  - The site-rollup line only says "Weakest pages" when a page actually scores below the site value; otherwise it reads "Pages by score" / "Páginas por puntuación".
  - `skills/score/SKILL.md` lists `--validate-only` (it was in `docs/{en,es}/usage.md` only).
- Regression coverage: `tests/unit/jsonld.test.mjs` (references vs declarations), `tests/unit/checks.test.mjs` (itemReviewed back-reference, ProductGroup eligibility, dated permalink vs date archive), `tests/unit/score.test.mjs` (language) and `tests/e2e/report.test.mjs` (Spanish chrome + the rollup label).
- Gates green: 144 files syntax-checked, versions aligned at 0.2.0, 34 skills linted, 1098 tests passing (43 files).

## Done (verification-round fix batches committed — 2026-09-06)
- All verification-round changes (rounds 1 and 2 above) are now committed on `main`; gates re-run on the committed tree: `node scripts/check.mjs` OK (144 files, 3 JSON, versions aligned at 0.2.0, 34 skills, 0 warnings) and `node tests/run.mjs` 1098 passed / 0 failed (43 files).

## Done (final verification round 3 — false-claim and credential fixes — 2026-09-06)
- One shared rule for "which JSON-LD nodes declare a product here": `scripts/lib/jsonld.mjs` gains
  `PRODUCT_TYPES`, `VARIANT_PROPS`, `PRODUCT_SUBSTANCE_PROPS`, `isProductNode()`, `isVariantEntry()`,
  `hasProductSubstance()`, `isProductDeclaration()`, `classifyProductNodes()` and
  `productDeclarations()`. `scripts/checks/{schema,ecommerce,platform-shopify}.mjs` all read it, so a
  `ProductGroup` with url-only `hasVariant` stubs (the allbirds.com shape: 20 stubs + one full
  variant + an `aggregateRating.itemReviewed`) is ONE declared product and produces none of
  `M5.product.missing_offer_price`, `M5.shopify.duplicate_product_jsonld`, `M5.shopify.theme_jsonld_gaps`
  or a failing `M18.agentic.catalog_eligibility`. A ProductGroup is judged together with its variants
  (`declarationGroup()` in the Shopify check) and priced from their offers (`offerFor()` source
  `variants`) when it carries no `offers` of its own.
- `M17.wordpress.duplicate_sitemaps` no longer calls one sitemap three: `scripts/lib/sitemaps.mjs`
  records `final_url`/`redirected`/`alias_of` per file and never walks or re-collects an alias (the
  techcrunch.com `/wp-sitemap.xml` -> `/sitemap_index.xml` -> `/sitemap.xml` chain), `found` and
  `crawl.mjs`'s `probe_hits` exclude aliases, and the check (`sitemapSources()`, `sameUrlSet()`)
  claims duplication only when two different documents serve materially different URL sets —
  `directional` when only one list was collected, `pass` with the evidence when they are aliases or
  identical. Evidence grammar is count-aware (no more "A and B and C both …").
- `scripts/fix-plan.mjs` `resolveTargets()` re-evaluates readiness from the passed `env` (default
  `process.env`) via `lib/credentials.mjs`, keeps the profile's tool detection (overridable with
  `tools`), returns `readiness[]`, and the dry-run summary prints ready/needs/missing per adapter —
  key names only. `--targets auto` now selects an adapter whose keys were exported after the audit.
- `scripts/lib/platform-rules.mjs` `ADAPTERS[].needs` and `PAGE_API_KEYS` name the catalog/`userConfig`
  keys; `detect-platform.mjs` `hasCredential()` resolves aliases through `lookupOrder()`, so both
  spellings still work. Reference cards and `references/routing.md` updated.
- `scripts/checks/index.mjs` no longer re-adds the snapshots `crawl.mjs` dropped as non-HTML.
- `scripts/checks/ai-crawlers.mjs` `isDocumentedBot()`: an `established` `M14.citation_bots.blocked`
  needs at least one vendor-documented token; a block reaching only legacy ones is `directional`.
- `README.md` and `docs/{en,es}/usage.md` mark "git-aware" as writer protocol rather than a
  code-enforced guarantee; `docs/{en,es}/usage.md` document the live credential re-check.
- Regression coverage: `tests/unit/jsonld.test.mjs` (product declarations), `tests/unit/checks.test.mjs`
  (the allbirds shape, a real duplicate, variant-priced groups, four sitemap-source cases, the
  undocumented-token downgrade, the non-HTML snapshot), `tests/unit/sitemaps.test.mjs` (redirect
  alias), `tests/unit/fix-plan.test.mjs` (env-injected readiness + the printed block),
  `tests/unit/detect-platform.test.mjs` (catalog names, alias resolution), `tests/e2e/checks-run.test.mjs`
  and `tests/e2e/{crawl,robots-sitemaps}.test.mjs` (the fixture server now redirects `/wp-sitemap.xml`).
- Gates green: 144 files syntax-checked, versions aligned at 0.2.0, 34 skills linted, 1115 tests
  passing (43 files).

## Done (final verification round 4 — sibling-node matching and evidence wording — 2026-09-06)
- Variant membership is object identity, never path text. `flattenNodes()` names a root after its
  type, so two sibling root `ProductGroup`s (a theme and an app each emitting one — the shape
  `M5.shopify.duplicate_product_jsonld` reports) both flatten to `"ProductGroup"`. Matching with
  `path.startsWith(entry.path + '.hasVariant')` therefore priced one group from the OTHER group's
  cheapest variant offer (`M18.agentic.catalog_eligibility` / `M18.offer.price_feed_mismatch` stated
  10 while the page rendered 500) and judged every group against the union of all variants
  (`M5.shopify.theme_jsonld_gaps` named a one-variant group as having 4, borrowed group B's `gtin13`
  to clear group A, and reported a brand that was not there — at `established` confidence).
  `scripts/lib/jsonld.mjs` gains `variantNodeSet(node)` (every object under the node's own
  `hasVariant`, keyed by identity); `offerFor()` and `declarationGroup()` use it.
- `scripts/lib/jsonld.mjs` `disambiguateRootPaths(entries)` renames repeated root paths
  (`ProductGroup` -> `ProductGroup[1]`, descendants following) so evidence points at one node;
  `schema.mjs`, `ecommerce.mjs` and `platform-shopify.mjs` apply it when they collect a page's nodes.
  It is presentation only — paths are still never used to decide membership.
- `M5.shopify.theme_jsonld_gaps` counts the Product/ProductGroup entries of a group, not its size:
  the allbirds group's 83 variants (+14 nested Offer nodes) were reported as "97 variant nodes".
- `M14.citation_bots.blocked` takes the singular verb for a single excluded token ("it is listed as
  evidence but does not carry the claim").
- `M9.alt.missing` stops quoting a tag the page does not ship: `scripts/lib/html.mjs` records
  `images[].bound` (framework-bound `src`/`alt` — `:src`, `v-bind:alt`, `x-bind:src`, `[src]`), the
  quoted tag omits an absent `src` instead of printing `src=""`, and the evidence names the binding
  so the static-render caveat is visible. The claim itself stands for a non-JS consumer.
- Regression coverage: `tests/unit/jsonld.test.mjs` (sibling roots — identity matching, cycles, root
  renaming across blocks), `tests/unit/checks.test.mjs` (two root ProductGroups priced and judged
  separately, the framework-bound image, singular/plural citation-bot grammar),
  `tests/unit/html.test.mjs` (`bound` attribute capture).
- Gates green: 144 files syntax-checked, versions aligned at 0.2.0, 34 skills linted, 1123 tests
  passing (43 files).

## Remaining (in this order; commit after each, keep `node scripts/check.mjs && node tests/run.mjs` green)
- Release mechanics only: `claude plugin validate . --strict` if available, then tag and publish.

## Conventions that must not drift
Zero-dependency Node ≥18 ESM; every CLI exports `main(args) → {result, code}` with the `isMain` guard (no `process.exit` outside the two hook scripts, `scripts/guard-write.mjs` and `scripts/guard-bash.mjs`); `node:test` tests only against the local fixture server; findings schema-valid via `makeFinding`; honesty guardrails (llms.txt/M21 weight 0, `established` only when a vendor documents it, `needs_api`/`manual_review` never a silent pass, no fabricated values); read-only auditors / single writer; never write live by default; bilingual docs; original text only.
